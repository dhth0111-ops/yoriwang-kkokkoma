# 요리왕 꼬꼬마 V3.6 Firebase

V3.5 Dev Fix4를 기준으로 Firebase 익명 인증과 Firestore 클라우드 동기화를 추가한 전체 버전입니다.

## 사용 순서
1. GitHub Pages에 전체 파일을 덮어씁니다.
2. Firebase Authentication의 익명 로그인을 활성화합니다.
3. `firestore.rules` 내용을 Firestore 규칙에 게시합니다.
4. 앱 설정 → Firebase 클라우드 → 현재 자료 클라우드에 올리기를 한 번 실행합니다.
5. 다른 기기에서는 클라우드 자료 불러오기를 실행합니다.

## 데이터 보호
- 기존 IndexedDB 로컬 저장 유지
- JSON 전체 백업/복원 유지
- Firestore에는 큰 사진을 포함한 전체 JSON을 여러 조각으로 나누어 저장
- 최초 클라우드 업로드 이후 로컬 변경사항 자동 업로드

# 요리왕 꼬꼬마 V3.5 Dev Fix4

V3.4 Stable을 기반으로 정리한 개발판 전체 프로젝트입니다.

## 주요 기능
- 설정 화면 정상 표시
- 장보기 제품 사진 추가·변경·삭제·크게 보기
- 구매처 여러 곳과 구매 링크 저장
- 기본자료 저장·초기화
- 사진을 포함한 전체 JSON 백업·복원

## Fix4 수정 사항
- 레시피 목록 사진의 고정 높이와 `object-fit: cover` 제거
- 레시피 상세 사진과 등록 미리보기의 원본 비율 유지
- 휴대폰에서 편집해 올린 세로·가로·정사각형 사진을 추가로 자르지 않고 표시
- 장보기 썸네일도 잘리지 않도록 `contain` 방식 적용
- `index.html`, `app.js`, `style.css`, `manifest.json`, `service-worker.js` 버전을 Fix4로 통일
- 서비스 워커 캐시 이름을 Fix4로 갱신하고 이전 캐시 자동 삭제

## 새로 배포하는 방법
1. JSON 백업 파일을 별도로 보관합니다.
2. Dev 저장소의 기존 프로젝트 파일을 삭제하고 커밋합니다.
3. 이 ZIP의 내용 전체를 저장소 최상위에 업로드하고 다시 커밋합니다.
4. GitHub Pages 배포 완료 후 기존 설치 앱을 삭제하고 새로 설치합니다.


## V3.6.2 수정 사항
- Firestore 요청 10MiB 초과 오류 수정
- 사진 포함 전체 자료를 작은 조각으로 나눠 여러 요청으로 업로드
- 업로드·불러오기 진행률 표시
- 클라우드 저장 완료 후 자동 동기화 활성화
- 카드 배경 고정색 유지


## V3.6.3 수정
- Firebase 대용량 업로드가 화면 종료/네트워크 끊김으로 중단되면, 같은 자료인 경우 완료된 조각부터 이어서 업로드합니다.
- 레시피 카드의 본문(card-body) 배경까지 #f7f7f7 고정색으로 적용해 사진 밝기와 무관하게 동일하게 표시합니다.
- 서비스워커 캐시 버전을 갱신했습니다.

## V3.6.4 수정
- V3.6.3의 중단 후 이어올리기 및 자동 클라우드 저장 유지
- 레시피 카드 본문 배경을 순백색(#ffffff)으로 강제 고정
- 카드 본문의 background-image/filter/backdrop-filter/pseudo 배경 제거
- 서비스워커 캐시 버전 갱신
