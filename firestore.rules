rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sharedBackups/{backupId} {
      allow read, write: if request.auth != null;
      match /chunks/{chunkId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
